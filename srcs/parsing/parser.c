#include "minishell.h"

void	parse(char *line)
{
	// skip space
	// get command
	// skip space
	// until it doesn't start by '-' + alphanumerical (until stop alphanumerical, get flag
	// skip space
	// check redir
	// check quote
	// save arg
	printf("%s\n", line);
	return ;
}